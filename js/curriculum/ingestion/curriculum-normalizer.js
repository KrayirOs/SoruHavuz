export function normalizeCurriculumText(value) { return String(value ?? "").replace(/(\p{L})-\s*\n\s*(\p{L})/gu, "$1$2").replace(/\s+/g, " ").trim(); }
export function normalizeExtractedRecord(record) { return { ...record, exam: normalizeCurriculumText(record.exam) || null, subject: normalizeCurriculumText(record.subject) || null, grade: normalizeCurriculumText(record.grade) || null, unit: normalizeCurriculumText(record.unit) || null, topic: normalizeCurriculumText(record.topic) || null, code: normalizeCurriculumText(record.code) || null, text: normalizeCurriculumText(record.text), explanations: (record.explanations || []).map(normalizeCurriculumText).filter(Boolean) }; }
